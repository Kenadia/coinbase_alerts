{
  "name": "coinbase_alerts",
  "version": "0.1.0",
  "main": "dist/src/index.js",
  "scripts": {
    "compile": "tsc index.ts",
    "compile:watch": "npm run compile -- --watch",
    "start": "while true; do node .; sleep 5; done",
    "test": "TEST_TRIGGER=true node ."
  },
  "devDependencies": {
    "@types/node": "^13.9.1",
    "typescript": "^3.8.3"
  }
}
